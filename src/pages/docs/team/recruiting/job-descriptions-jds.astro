---
import { getEntry } from 'astro:content';
import Layout from '../../../../layouts/Layout.astro';
import JobCard from '../../../../components/JobCard.astro';

const entry = await getEntry('docs', 'team/recruiting/job-descriptions-jds');
const { Content } = await entry.render();

const breadcrumbs = [
  { text: 'Recruiting Team', href: '/docs/team/recruiting' }
];

const productRoles = [
  {
    title: 'Product Design',
    description: 'UX Designer job description template',
    href: '/docs/team/recruiting/job-descriptions-jds/ux-designer',
    icon: '🎨'
  }
];

const creativeRoles = [
  {
    title: "Graphic Designer",
    description: "Design stunning visuals and branding materials.",
    href: "/job-descriptions-jds/graphic-designer",
    icon: "🎨"
  },
  {
    title: "Copywriter",
    description: "Write compelling content to captivate and engage your audience.",
    href: "/job-descriptions-jds/copywriter",
    icon: "✍️"
  }
];

const title = entry.data.title;
---

<Layout title={title}>
  <div class="py-8 px-4 sm:px-6 lg:px-8">
    <div class="max-w-[680px] mx-auto">
      <nav class="flex mb-2" aria-label="Breadcrumb">
        <ol class="flex items-center">
          {breadcrumbs.map((crumb) => (
            <li>
              <a href={crumb.href} class="text-sm text-gray-600 hover:text-gray-900">
                {crumb.text}
              </a>
            </li>
          ))}
        </ol>
      </nav>

      <div class="mb-12">
        <h1 class="text-[32px] font-semibold text-[#1a1f36] tracking-[-0.4px]">Job Description Templates</h1>
        <div class="gradient-panel">
          <p class="text-[17px] leading-7 text-[#1a1f36]">
            Well-crafted job descriptions are essential for attracting the right talent and setting clear expectations. These templates provide a consistent framework for creating effective JDs.
          </p>
        </div>
      </div>

      <div class="space-y-12">
        <section>
          <h2 class="text-xl font-semibold text-[#1a1f36] mb-6">Product Role JDs</h2>
          <div class="grid grid-cols-1 gap-4">
            {productRoles.map(role => (
              <JobCard {...role} />
            ))}
          </div>
        </section>

        <section>
          <h2 class="text-xl font-semibold text-[#1a1f36] mb-6">Creative Role JDs</h2>
          <div class="grid grid-cols-1 gap-4">
            {creativeRoles.map(role => (
              <JobCard {...role} />
            ))}
          </div>
        </section>
      </div>
    </div>
  </div>
</Layout>