---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`bg-[#f6f8fa] border border-[#e5e7eb] rounded-lg p-6 my-8 ${className || ''}`}>
  <slot />
</div>
