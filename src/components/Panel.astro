---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`bg-white/30 backdrop-blur-[2px] rounded-xl p-8 my-8 ring-2 ring-[#D4BFE3] shadow-[0_0_0_1px_rgba(212,191,227,0.3)] ${className || ''}`}>
  <slot />
</div>
