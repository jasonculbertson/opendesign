---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`bg-white/40 backdrop-blur-[2px] rounded-xl p-8 my-8 ring-1 ring-[#F4E5E9] shadow-[0_0_1px_rgba(244,229,233,0.5)] ${className || ''}`}>
  <slot />
</div>
