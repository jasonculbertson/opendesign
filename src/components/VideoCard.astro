---
interface Props {
  title: string;
  timestamp: string;
  duration: string;
  thumbnailUrl: string;
  isDraft?: boolean;
}

const { title, timestamp, duration, thumbnailUrl, isDraft = false } = Astro.props;
---

<div class="group relative rounded-xl overflow-hidden bg-white border border-[#e5e7eb] hover:border-[#d1d5db] transition-colors">
  <div class="aspect-video relative overflow-hidden">
    <img 
      src={thumbnailUrl} 
      alt={title}
      class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
    />
    <div class="absolute bottom-2 right-2 px-2 py-1 bg-black/70 rounded text-white text-[13px] font-medium">
      {duration}
    </div>
    {isDraft && (
      <div class="absolute top-3 right-3 px-3 py-1 bg-[#f97316] rounded-lg text-white text-[13px] font-medium">
        New
      </div>
    )}
  </div>
  <div class="p-4">
    <h3 class="text-[15px] font-semibold text-[#1a1f36] mb-1">{title}</h3>
    <p class="text-[13px] text-[#3c4257]">{timestamp}</p>
  </div>
</div>
